# GENERATED by make_variant_configs.py -- edit config.yaml, not this file.
# dokls ablation config. heavy data offloaded to ${DPE_DATA_ROOT}; light
# results/figures repo-relative (gitignored). no cluster-absolute paths.
data_dir: "${DPE_DATA_ROOT}/dokls/data"
raw_results_dir: "ex/ablations/dokls/raw_results"
processed_results_dir: "ex/ablations/dokls/processed_results"
figures_dir: "ex/ablations/dokls/figures"

data_dim: 3
kl_distances: [0.3, 1, 3, 9, 18, 36, 54]
num_instances_per_kl: 10
nsamples_max: 8192
nsamples_grid: [1024, 2048, 4096, 8192]

device: "cuda"
seed: 1729

# per-run selection for step2 (env DPE_DOKLS_ROUTE/PSTAR_IDX/NSAMPLES/NSTAR override these).
# nsamples = N (p0/p1 budget); nstar = Nstar (p* budget); equal on the diagonal.
route: two_leg
pstar_idx: 0
nsamples: 2048
nstar: 2048

winners_path: "ex/ablations/dokls/winners"
